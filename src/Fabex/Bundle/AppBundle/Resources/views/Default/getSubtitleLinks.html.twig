{% for provider, subtitle in subtitles %}
    <div>
        <h4>{{ provider }}</h4>
        {% for sub in subtitle %}
            <a class="subtitleLink" data-url="{{ sub.file }}" href="{{ sub.file }}">{{ sub.name }}</a> <br/>
        {% endfor %}
    </div>
{% endfor %}
